# JitPack build configuration
jdk:
  - openjdk17

before_install:
  - sdk install java 17.0.13-tem || true
  - sdk use java 17.0.13-tem || true
  - sdk install maven 3.9.6 || true
  - sdk use maven 3.9.6 || true

install:
  - mvn install -DskipTests -Dstyle.color=never
